# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lock files: project uses bun (bun.lockb is canonical for Lovable Cloud)
# Exclude conflicting lock files generated by other package managers
package-lock.json
yarn.lock
pnpm-lock.yaml
# Exclude bun text lockfile (bun.lockb binary is used by Lovable)
bun.lock

# Environment & Secrets (AIOX)
*.key
*.pem

# Dependencies (AIOX)
node_modules/

# Build & Logs (AIOX)
dist/
build/
logs/

# IDE & OS (AIOX)
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
.vercel

# Supabase CLI local cache
supabase/.temp/
.env*.local
